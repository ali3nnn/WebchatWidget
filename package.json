{
  "name": "webchatwidget",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "esbuild src/webchat.ts --bundle --outfile=dist/webchat.js --loader:.css=text --format=iife --sourcemap --watch",
    "build": "esbuild src/webchat.ts --bundle --outfile=dist/webchat.js --loader:.css=text --format=iife --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.25.5",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.5",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "parcel": "^2.15.2",
    "rollup": "^2.79.2",
    "rollup-plugin-terser": "^7.0.2"
  },
  "targets": {
    "default": {
      "distDir": "dist",
      "sourceMap": false,
      "engines": {
        "browsers": "> 0.25%, not dead"
      },
      "outputFormat": "global",
      "isLibrary": true,
      "includeNodeModules": true
    }
  }
}
